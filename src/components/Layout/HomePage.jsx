import GameRoom from "../Menu/GameRoom";
import SideBar from "./SideBar";

const HomePage = (props) => {
  return (
    <div>
      <GameRoom />
      <SideBar />
    </div>
  );
};

export default HomePage;
